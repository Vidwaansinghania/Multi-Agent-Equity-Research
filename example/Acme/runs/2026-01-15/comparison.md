---
stage: 7
---

# Method 2 against the single-analyst node — ACME

Acme Materials Corporation is invented. This file shows what a comparison covers
when the same ticker has been through both processes.

## The two calls

| | Single-analyst process | Method 2 |
|---|---|---|
| Call date | none | 2026-01-15 |
| Rating | no coverage | HOLD |
| Target | — | $108.50 |
| Reference price | — | $100.00 |

There is no prior single-analyst node for ACME, so this run cannot test whether
adversarial pressure changes a call. The column stays empty until one exists.

## What the fan-out produced that a single pass would not

Two things.

The bear rebuilt the valuation model to check it, found that the terminal
reinvestment rate and the sensitivity grid both reconcile, and recorded that no
error existed. A negative result from an adversarial check is worth having on the
record.

The risk gate priced the currency round trip at 3.8% against an expected return of
+10.7%, which turns an unqualified hold into a hold with a stated net figure. On a
call inside the band that arithmetic decides whether acting on it pays at all.

## Where the two processes would be expected to differ

The cost of capital, which is where two methods on one ticker usually diverge, and
the bear probability, which here is the only input that moves the rating across a
band edge.
